// You can use the function from the `Math` module.

const sqrt = (num) => Math.sqrt(num);
const power = (num, x) => Math.pow(num, x)
const round = (num) => Math.round(num)

// Don't change the exported names. 
export {
  sqrt,
  power,
  round,
};
